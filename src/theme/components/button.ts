import { Components } from '@mui/material';

const muiButton: Components['MuiButton'] = {
   styleOverrides: {
      root: {
         textTransform: 'none',
      },
   },
};

export default muiButton;
